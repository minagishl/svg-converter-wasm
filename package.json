{
  "name": "svg-converter-wasm",
  "version": "1.0.0",
  "description": "Convert SVG to PNG and JPEG using WebAssembly",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "wasm-pack build --target web && node scripts/build.mjs",
    "format": "prettier --write ./src ./scripts"
  },
  "author": "Minagishl",
  "license": "MIT",
  "dependencies": {
    "binaryen": "^118.0.0",
    "esbuild": "^0.23.0",
    "esbuild-plugin-d.ts": "^1.3.0",
    "prettier": "^3.3.3"
  }
}
